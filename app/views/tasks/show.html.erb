<h1> <%= @task.title %> </h1>

<p>Details: <%= @task.details %> </p>
<p> <% if @task.completed == true %>
      <i class="far fa-check-square"></i> The task is completed
    <% else %>
      <i class="far fa-square"></i> The task needs to be done
    <% end %> </p>
<br>
<%= link_to "Edit the task", edit_task_path(@task) %>
<br>
<br>
<i class="fas fa-undo-alt"></i> <%= link_to "Back to list", tasks_path %>
